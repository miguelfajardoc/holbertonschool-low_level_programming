#include "holberton.h"
/**
 * add - add two digits and returns the result
 * @x: the first number
 * @y: the second number
 * Return: the result of the sum
 */

int add(int x, int y)
{
	return (x + y);
}
